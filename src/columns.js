//const columns=[{id, companyId,companyName, email, phoneNumber,contactName,adress,companyIdCRM, manuallyProcessing, payforSubscribtion, payForCloud, payForNtrip,isAdmin}];
//export {columns};

/* const columns = [{ field: "companyId", headerName: "Company ID", flex: 0.5 },
    {
      field: "email",
      headerName: "E-Mail",
      flex: 1
    },
    { field: "phoneNumber", headerName: "Phone Number", flex: 1 },
    {
      field: "contactName",
      headerName: "Contact",
      flex: 1
    },
    {
      field: "Other Information",
      flex: 0.5,
      //  renderHeader: () => (
      //     <VisibilityIcon color="primary" />
      // ),
      renderCell: (params) => (
        <VisibilityIcon />

        // <SimpleModal
        //   row={params}
        //   onClick={console.log("On click")}
        //   onRowClick={() => console.log("Render Cell")}
        // />
      )
    }
  ]; */

  